function fizzbuzz(num) {

  for (i = 1; i <= num; i++) {
    var result = "";
    if (i % 3 == 0 || i % 5 == 0) {
      result += "Fizz";
    }
    console.log(i + " " + result);
  }
}

fizzbuzz(100);
